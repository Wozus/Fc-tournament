import "./globals.css";

export const metadata = {
  title: "Fife tiles",
  description: "Excel → kafelki wyników",
};

export const viewport = {
  width: "device-width",
  initialScale: 1,
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="pl">
      <body>{children}</body>
    </html>
  );
}
